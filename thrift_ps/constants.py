#HOST = '172.31.37.151'  # Standard loopback interface address (localhost)
HOST = '127.0.0.1'  # Standard loopback interface address (localhost)
PORT = 9000             # Port to listen on (non-privileged ports are > 1023)
INTERVAL = 60
EXPIRED_TIME = 600
MONITOR_COUNTER = 100
